<p>
  <b>Editor:</b>
  <%= @editors.find{|e| e.id == @profile.editor_id}.email %>
</p>

<p>
  <b>Title:</b>
  <%= @profile.title %>
</p>

<p>
  <%= raw @profile.content %>
</p>


<%if editor_signed_in?%>
<%= link_to 'Edit', edit_profile_path(@profile) %> |
<%end%>
<%= link_to 'Back', profiles_path %>
